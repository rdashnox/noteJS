# MongoDB Atlas (Free Cluster) Connection String:
# 1. Go to https://cloud.mongodb.com and open your project.
# 2. Create (or select) a Free Shared Cluster (M0).
# 3. In Network Access, add IP Address: 0.0.0.0/0 (Allow access from anywhere).
# 4. In Database Access, create a database user with read/write access.
# 5. Click "Connect" → "Connect your application" → select Node.js and copy the URI.
# 6. Add your own database name after `.net/` (it does NOT need to exist in Atlas).
#    MongoDB will automatically create the database when your app writes data.
#    Choose a clear dbname (e.g. myapp, myapp_dev, etc.)
# 7. Replace <username>, <password>, and <dbname> below.

MONGO_URI=mongodb+srv://dash:<password>@dash.y0c8al4.mongodb.net/
PORT=3000 # you can change to any port